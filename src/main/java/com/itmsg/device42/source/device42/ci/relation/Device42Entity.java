package com.itmsg.device42.source.device42.ci.relation;

/** PK의 이름 공간. 서로 다른 D42 뷰의 같은 숫자를 구별한다. */
public enum Device42Entity {
    DEVICE, DEVICEOS, PART, MOUNTPOINT, DATABASEINSTANCE, IPADDRESS
}
